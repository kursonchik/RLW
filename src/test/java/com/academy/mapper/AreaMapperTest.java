package com.academy.mapper;

import com.academy.dto.AreaDto;
import com.academy.model.entity.Areas;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;

/**
 * @author : Volha Salash
 */
/*
class AreaMapperTest {
    @Test
    void givenAreas_whenMaps_thenCorrect() {
        Areas entity = new Areas();
        entity.setId(1);
        entity.setDistance(5);

        AreaDto areaDto = AreaMapper.INSTANCE.toDto(entity);

        assertThat(areaDto.getId()).isEqualTo(entity.getId());
        assertThat(areaDto.getDistance()).isEqualTo(entity.getDistance());
    }
}

 */
